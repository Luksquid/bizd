CREATE OR REPLACE TRIGGER STANOWISKO_SPRAWDZ_MIEJSCE
BEFORE INSERT OR UPDATE ON STANOWISKO
FOR EACH ROW
BEGIN
    IF :NEW.MIEJSCE != 'bar' AND :NEW.MIEJSCE != 'ob' THEN
        RAISE_APPLICATION_ERROR(-20008, 'Stanowisko może przyjąć tylko wartość ob lub bar.');
    END IF;
END;
