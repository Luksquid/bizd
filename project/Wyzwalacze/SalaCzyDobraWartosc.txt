CREATE OR REPLACE TRIGGER SALA_CZY_DOBRA_WARTOSC
BEFORE INSERT OR UPDATE ON SALA
FOR EACH ROW
BEGIN
    IF :NEW.AKTYWNA > 1 OR :NEW.AKTYWNA < 0 THEN
        RAISE_APPLICATION_ERROR(-20002, 'Niewłaściwa wartość. Sala może być tylko aktywna(1) lub nieaktywna(0)');
    END IF;
END;

